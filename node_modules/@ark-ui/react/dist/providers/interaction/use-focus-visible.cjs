"use client";
const require_use_environment_context = require("../environment/use-environment-context.cjs");
let react = require("react");
let _zag_js_focus_visible = require("@zag-js/focus-visible");
//#region src/providers/interaction/use-focus-visible.ts
function useFocusVisible(props = {}) {
	const { isTextInput, autoFocus } = props;
	const { getRootNode } = require_use_environment_context.useEnvironmentContext();
	const subscribe = (0, react.useCallback)((onStoreChange) => (0, _zag_js_focus_visible.trackFocusVisible)({
		root: getRootNode(),
		isTextInput,
		autoFocus,
		onChange: onStoreChange
	}), [
		getRootNode,
		isTextInput,
		autoFocus
	]);
	const getSnapshot = (0, react.useCallback)(() => autoFocus || (0, _zag_js_focus_visible.isFocusVisible)(), [autoFocus]);
	return (0, react.useSyncExternalStore)(subscribe, getSnapshot, () => false);
}
//#endregion
exports.useFocusVisible = useFocusVisible;
