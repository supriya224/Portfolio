"use client";
import { useRenderStrategyPropsContext } from "../../utils/render-strategy.js";
import { useComposedRefs } from "../../utils/compose-refs.js";
import { ark } from "../factory.js";
import { PresenceGate } from "../presence/presence-gate.js";
import { usePresence } from "../presence/use-presence.js";
import { PresenceProvider } from "../presence/use-presence-context.js";
import { useNavigationMenuContext } from "./use-navigation-menu-context.js";
import { useNavigationMenuViewportPropsContext } from "./use-navigation-menu-viewport-props-context.js";
import { forwardRef } from "react";
import { mergeProps } from "@zag-js/react";
import { jsx } from "react/jsx-runtime";
//#region src/components/navigation-menu/navigation-menu-viewport.tsx
var NavigationMenuViewport = forwardRef((props, ref) => {
	const viewportPropsContext = useNavigationMenuViewportPropsContext();
	const navigationMenu = useNavigationMenuContext();
	const renderStrategyProps = useRenderStrategyPropsContext();
	const presence = usePresence({
		...renderStrategyProps,
		present: navigationMenu.open
	});
	const mergedProps = mergeProps(navigationMenu.getViewportProps(viewportPropsContext), presence.getPresenceProps(), props);
	const composedRefs = useComposedRefs(presence.ref, ref);
	return /* @__PURE__ */ jsx(PresenceProvider, {
		value: presence,
		children: /* @__PURE__ */ jsx(PresenceGate, {
			presence,
			children: /* @__PURE__ */ jsx(ark.div, {
				...mergedProps,
				ref: composedRefs
			})
		})
	});
});
NavigationMenuViewport.displayName = "NavigationMenuViewport";
//#endregion
export { NavigationMenuViewport };
