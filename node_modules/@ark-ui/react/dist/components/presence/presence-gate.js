"use client";
import { Activity } from "../../utils/react-activity.js";
import { jsx } from "react/jsx-runtime";
//#region src/components/presence/presence-gate.tsx
var PresenceGate = (props) => {
	const { presence, children } = props;
	if (presence.unmounted) return null;
	if (presence.hideMode === "activity" && Activity) return /* @__PURE__ */ jsx(Activity, {
		mode: presence.present ? "visible" : "hidden",
		children
	});
	return children;
};
//#endregion
export { PresenceGate };
