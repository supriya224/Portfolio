/** Create a value once for the component lifetime. */
declare function useConst<T>(factory: () => T): T;
/** Stable function identity that always calls the latest implementation. */
declare function useStableFn<T extends (...args: any[]) => any>(fn: T): T;

export { useConst, useStableFn };
