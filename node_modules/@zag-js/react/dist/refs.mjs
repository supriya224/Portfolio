"use client";

// src/refs.ts
import { useRef } from "react";
import { useConst } from "./stable.mjs";
function useRefs(refs) {
  const ref = useRef(refs);
  return useConst(() => ({
    get(key) {
      return ref.current[key];
    },
    set(key, value) {
      ref.current[key] = value;
    }
  }));
}
export {
  useRefs
};
